% Pakete
  % Sprache
    % \usepackage{usc} % Veraltet, nicht mehr notwendig
    % Unterstützung von Unicode Text
    \usepackage[utf8]{inputenc}
    % \usepackage[ngerman]{babel}
    % \usepackage[english]{babel}
    % Schönere Schriftart
    \usepackage{lmodern}
    % Griechisch
    % \usepackage{textgreek}
    % \usepackage{pxfonts}
  %
  % Sonstige
    \usepackage[
      pdfborder={0 0 0} % Keine roten Kästen um die Referenzen
    ]{hyperref}
    \usepackage{autonum}
    \usepackage{csquotes}
      % Bsp: \enquote{abc}
    % Farben
    \usepackage{color}
    \usepackage{xcolor}
    % Labelanpassungen bei Enumerationen
    \usepackage{enumitem}
    % Umrahmungen
    % \usepackage{framed} % Veraltet: Ich werwend mdframed
    \usepackage{mdframed}
    % if Abfragen
    \usepackage{ifthen}
    % Code
    \usepackage{listings}
    % Stilistische Anpassungen
    \usepackage{geometry}
      \geometry{
        a4paper,
        % left=20mm,
        % right=25mm,
        % bottom=30mm,
        % headsep=10mm,
        % footskip=12mm
      }
  %
%

% Kommandos - noch sortieren
  \newcommand{\dateref}[3]{
  %   % Takes: day - month - year
    \begin{mdframed}[
      backgroundcolor=gray!10
    ]
      This lecture took place on #3/#2/#1
    \end{mdframed}
  }
  % TODO Creates "overful hbox" warning...
  \newcommand{\myline}{\noindent\rule[0.5ex]{\linewidth}{0.5pt}\\}
  \newcommand{\normchris}[2][]{
    \left|\left|
      \ifthenelse{\equal{#2}{}}{\cdot}{#2}
    \right|\right|_{#1}
  }
  \newcommand{\topol} {
    % TODO: Find a better symbol. This should be a "großes Tau"
    \mathcal T
  }
  \newcommand{\eqc}[1]{\stackrel{#1}{=}}

% -------------------------------------------- Worked through here ----------------------------------------------------
  \newcommand{\mymatrix}[1]{
    \begin{matrix}
      #1
    \end{matrix}
  }   
  \newcommand{\matrixe}[1]{
    \left[\begin{matrix}
      #1
    \end{matrix}\right]
  }
  \newcommand{\detmatrix}[1]{
    \left|\begin{matrix}
      #1
    \end{matrix}\right|
  }
  \newcommand{\matrixr}[1]{
    \left(\begin{matrix}
      #1
    \end{matrix}\right)
  }

  \newcommand{\learn}[1]{\textcolor{red}{#1}}
  \newcommand{\len}[1]{\left|#1\right|}
  \newcommand{\id}{\textnormal{id}}
  \newcommand{\sign}{\textnormal{sign \,}}
  \newcommand{\wings}[1] {
    \left<#1\right>
  }
%

% Umgebungen
  % Definition - nummeriert
    \providecommand{\myDefCounter}{section}
    \ifthenelse{ % if
      \equal{\myDefCounter}{section}
    }{ % then
    }{ % else
      \def\themyDefCounter{\thechapter.\arabic{myDefCounter}}
    }
    % Set the reference to chapter.counter
    \newenvironment{Def}[1][] {
      % Step counter
      \refstepcounter{\myDefCounter}
      % Create frame
      % \setlength\FrameRule{0.5pt}
      % \begin{framed}
      % Title
      \ifthenelse{ 
        \equal{#1}{}
      } {
        % No own title
        \textbf{Definition \arabic{chapter}.\arabic{\myDefCounter}.}
      } {
        % Own title
        \textbf{Definition \arabic{chapter}.\arabic{\myDefCounter} (#1).}
      }
    } {
      % Close frame
      % \end{framed}
    }
  %
  % Definition - unnummeriert
    \newenvironment{Def2}[1][] {
      % \setlength\FrameRule{0.5pt}
      % \begin{framed}
      \ifthenelse{
        \equal{#1}{}
      } {
        \textbf{Definition.}
      % TODO Woher kommt der Abstand? -> Definition (foo). tab? text
      } {
        % Own title
        \textbf{Definition} (#1).
      }
    } {
      % \end{framed}
    }
  %
  % Definition rahmenlos unnummeriert
    \newenvironment{def2}[1][] {
        \textbf{Definition}
        \ifthenelse{
          \equal{#1}{}
        }{
        }{
          (#1)
        }
      } {

      }
  % Theorem - nummeriert
    \providecommand{\mytheoremCounter}{section}
    \ifthenelse{
      \equal{\mytheoremCounter}{section}
    }{
    }{
      \def\themytheoremCounter{\thechapter.\arabic{mytheoremCounter}}
    }
    % % Set the reference to chapter.counter
    % \newenvironment{theorem}[1][] {
    %   % Step counter
    %   \refstepcounter{\mytheoremCounter}
    %   % Create frame
    %   % \setlength\FrameRule{1pt}
    %   \mdfsetup{
    %     middlelinecolor=red,
    %     middlelinewidth=5pt,
    %     backgroundcolor=red!10,
    %     roundcorner=10pt
    %   }
    %   \begin{mdframed}
    %   % Title
    %   \ifthenelse{
    %     \equal{#1}{}
    %   } {
    %     % No own title
    %     \textbf{Theorem \arabic{chapter}.\arabic{\mytheoremCounter}.}
    %   } {
    %     % Own title
    %     \textbf{Theorem \arabic{chapter}.\arabic{\mytheoremCounter}} (#1).
    %   }
    % } {
    %   \end{mdframed}
    % }
  %
  % theorem - unnummeriert
    \newenvironment{theorem2}[1][] {
      % \setlength\FrameRule{1pt}
      % \begin{framed}
      \ifthenelse{
        \equal{#1}{}
      } {
        % No own title
        \textbf{Theorem.}
      } {
        % Own title
        \textbf{Theorem} (#1).
      }
    } {
      % \end{framed}
    }
  %
  % Satz - nummeriert
    \providecommand{\mysatzCounter}{section}
    \ifthenelse{
      \equal{\mysatzCounter}{section}
    } {
    } {
      \def\themysatzCounter{\thechapter.\arabic{mysatzCounter}}
    }
    % Set the reference to chapter.counter
    \newenvironment{satz}[1][] {
      % Step counter
      \refstepcounter{\mysatzCounter}
      % Create frame
      % \setlength\FrameRule{1pt}
      % \begin{framed}
      % Title
      \ifthenelse{
        \equal{#1}{}
      }{
        % No own title
        \textbf{Satz \arabic{chapter}.\arabic{\mysatzCounter}.}
      } {
        % Own title
        \textbf{Satz \arabic{chapter}.\arabic{\mysatzCounter}} (#1).
      }
    } {
      % \end{framed}
    }
  %
  % Satz - unnummeriert
    \newenvironment{satz2}[1][] {
      % \setlength\FrameRule{1pt}
      % \begin{framed}
      % Title
      \ifthenelse{
        \equal{#1}{}
      } {
        % No own title
        \textbf{Satz.}
      } {
        % Own title
        \textbf{Satz} (#1).
      }
    } {
      % \end{framed}
    }
  %
  % Lemma - nummeriert
    \providecommand{\mylemCounter}{section}
    \ifthenelse{
      \equal{\mylemCounter}{section}
    } {
    } {
     \def\themylemCounter{\theChapter.\arabic{mylemCounter}}
    }
    % Set the reference to chapter.counter
    \newenvironment{lem}[1][] {
      % Step counter
      \refstepcounter{\mylemCounter}
      % Create frame
      % \setlength\FrameRule{0.5pt}
      % \begin{framed}
      % Title
      \ifthenelse{
        \equal{#1}{}
      } {
        % No own title
        \textbf{Lemma \arabic{chapter}.\arabic{\mylemCounter}.}
      } {
        % Own title
        \textbf{Satz \arabic{chapter}.\arabic{\mylemCounter}} (#1).
      }
    } {
      % \end{framed}
    }
  %
  % Corollary - nummeriert
    \providecommand{\mycorCounter}{section}
    \ifthenelse{
      \equal{\mycorCounter}{section}
    } {
    } {
      \def\themycorCounter{\thechapter.\arabic{mycorCounter}}
    }
    % Set the reference to chapter.counter
    \newenvironment{cor}[1][] {
      % Step counter
      \refstepcounter{\mycorCounter}
      % Create Frame
      % \setlength\FrameRule{1pt}
      % \begin{framed}
      % Title
      \ifthenelse{
        \equal{#1}{}
      } {
        % No own title
        \textbf{Corollary \arabic{chapter}.\arabic{\mykorCounter}.}
      } {
        % Own title
        \textbf{Corollary \arabic{chapter}.\arabic{\mykorCounter}} (#1).
      }
    } {
      % \end{framed}
    }
  %
  % Korollar - nummeriert
    \providecommand{\mykorCounter}{section}
    \ifthenelse{
      \equal{\mykorCounter}{section}
    } {
    } {
      \def\themykorCounter{\thechapter.\arabic{mykorCounter}}
    }
    % Set the reference to chapter.counter
    \newenvironment{kor}[1][] {
      % Step counter
      \refstepcounter{\mykorCounter}
      % Create frame
      % \setlength\FrameRule{1pt}
      % \begin{framed}
      % Title
      \ifthenelse{
        \equal{#1}{}
      } {
        % No own title
        \textbf{Korollar \arabic{chapter}.\arabic{\mykorCounter}.}
      } {
        % Own title
        \textbf{Korollar \arabic{chapter}.\arabic{\mykorCounter}} (#1).
      }
    } {
      % \end{framed}
    }
  %
  % Beispiel - nummeriert
    \providecommand{\myBspCounter}{section}
    \ifthenelse{
      \equal{\myBspCounter}{section}
    } {
    } {
      \def\themyBspCounter{\thechapter.\arabic{myBspCounter}}
    }
    % Set the reference to chapter.counter
    \newenvironment{Bsp} {
      \refstepcounter{\myBspCounter}
      \textbf{Beispiel \arabic{chapter}.\arabic{\myBspCounter}.}
      \itshape
    } {

    }
  %
  % Example - unnummeriert
    \newenvironment{example}[1][] {
      \ifthenelse{
        \equal{#1}{}
      } {
        % No own title
        \textbf{Example.}
      } {
        \textbf{Example} (#1).
      }
    } {

    }
  %
  % Bemerkung - unnummeriert
    \newenvironment{bem2}[1][] {
      \ifthenelse{\equal{#1}{}} {
        % No own title
        \textbf{Bemerkung.}
      } {
        \textbf{Bemerkung} (#1).
      }
    } {

    }
  %
  % Remark - unnummeriert
    \newenvironment{remark}[1][] {
      \ifthenelse{\equal{#1}{}}{
        % No own title
        \textbf{Remark.}
      } {
        \textbf{Remark} (#1).
      }
    } {

    }
  %
  % Nummerierte Umgebung mit eigener Überschrift
    \providecommand{\mysurCounter}{section}
    \ifthenelse{
      \equal{\mysurCounter}{section}
    } {
    } {
      \def\themysurCounter{\thechapter.\arabic{mysurCounter}}
    }
    \newenvironment{sur}[1][] {

      \refstepcounter{\mysurCounter}
      \textbf{\arabic{chapter}.\arabic{\mysurCounter} #1}
    } {

    }
  %
  % Axiom - unnummeriert
    % TODO Umrahmen? In irgendeiner Weise hervorheben?
    \newenvironment{axiom2}[1][] {
      \ifthenelse{
        \equal{#1}{}
      } {
        % No own title
        \textbf{Axiom.}
      } {
        \textbf{Axiom} (#1).
      }
    } {

    }
  %
%
